# 코드 리뷰

인자: `$ARGUMENTS` (선택, 리뷰 대상 파일 경로 또는 빈 값)

예시:

- `/project:review` — 현재 git 변경사항 전체 리뷰
- `/project:review components/common/Button.tsx` — 특정 파일 리뷰

---

이 프로젝트의 컨벤션과 기술 스택을 기준으로 코드를 리뷰합니다.

## 처리 단계

1. `$ARGUMENTS`가 비어있으면 `git diff HEAD` 또는 `git diff --staged`로 변경사항을 확인합니다.
   `$ARGUMENTS`에 파일 경로가 있으면 해당 파일을 읽습니다.

2. 아래 체크리스트를 기준으로 리뷰합니다.

## 리뷰 체크리스트

### 구조 & 컨벤션

- [ ] 파일 위치가 프로젝트 구조에 맞는가? (`components/`, `hooks/`, `service/` 등)
- [ ] 파일명 컨벤션을 따르는가? (컴포넌트 PascalCase, 훅 camelCase+use 접두사 등)
- [ ] `@/` 경로 alias를 사용하는가? (상대경로 지양)

### React & Next.js

- [ ] `"use client"` 지시어가 필요한 곳에만 있는가?
- [ ] 서버 컴포넌트에서 클라이언트 훅(useState 등)을 사용하지 않는가?
- [ ] Next.js App Router 패턴을 올바르게 사용하는가?

### TypeScript

- [ ] `any` 타입을 피하고 있는가?
- [ ] 인터페이스/타입이 `types/` 폴더에 정의되어 있는가?
- [ ] Props 타입이 명확히 정의되어 있는가?

### 상태 관리

- [ ] 서버 상태는 React Query(`useQuery`/`useMutation`)를 사용하는가?
- [ ] 클라이언트 전역 상태는 Zustand를 사용하는가?
- [ ] 로컬 UI 상태는 `useState`를 사용하는가?

### API & Axios

- [ ] `@/lib/axios` 인스턴스를 사용하는가? (axios 직접 import 금지)
- [ ] 서비스 함수가 `service/` 폴더에 분리되어 있는가?
- [ ] API 훅이 `hooks/api/` 폴더에 있는가?

### 성능 & 안정성

- [ ] 불필요한 `useEffect`가 없는가?
- [ ] 의존성 배열이 올바른가?
- [ ] 메모이제이션(`useMemo`, `useCallback`)이 과도하거나 누락되지 않았는가?

### 보안

- [ ] 클라이언트에 노출되는 환경 변수에 `NEXT_PUBLIC_` 접두사가 있는가?
- [ ] 민감한 정보가 클라이언트 번들에 포함되지 않는가?

## 리뷰 출력 형식

```
## 코드 리뷰 결과

### ✅ 잘 된 점
- ...

### ⚠️ 개선 제안
- [파일명:줄번호] 문제 설명
  → 제안: 수정 방법

### 🔴 수정 필요
- [파일명:줄번호] 심각한 문제 설명
  → 수정 방법

### 📝 요약
전반적인 코드 품질과 주요 액션 아이템
```
